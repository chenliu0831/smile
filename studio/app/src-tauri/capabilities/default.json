{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the Smile Studio main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn"
  ]
}
